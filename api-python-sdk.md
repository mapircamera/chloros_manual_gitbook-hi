# एपीआई: पायथन एसडीके

**क्लोरोस पायथन एसडीके** क्लोरोस इमेज प्रोसेसिंग इंजन तक प्रोग्रामेटिक पहुंच प्रदान करता है, जो आपके पायथन अनुप्रयोगों और अनुसंधान पाइपलाइनों के साथ स्वचालन, कस्टम वर्कफ़्लो और निर्बाध एकीकरण को सक्षम करता है।

### प्रमुख विशेषताऐं

* 🐍 **नेटिव पायथन** - इमेज प्रोसेसिंग के लिए स्वच्छ, पायथोनिक एपीआई
* 🔧 **पूर्ण एपीआई एक्सेस** - क्लोरोस प्रसंस्करण पर पूर्ण नियंत्रण
* 🚀 **स्वचालन** - कस्टम बैच प्रोसेसिंग वर्कफ़्लोज़ बनाएं
* 🔗 **एकीकरण** - मौजूदा पायथन अनुप्रयोगों में क्लोरोस एम्बेड करें
* 📊 **अनुसंधान के लिए तैयार** - वैज्ञानिक विश्लेषण पाइपलाइनों के लिए बिल्कुल सही
* ⚡ **समानांतर प्रसंस्करण** - आपके सीपीयू कोर को स्केल करता है (क्लोरोस+)

### आवश्यकताएं

| आवश्यकता | विवरण |
| -------------------- | ------------------------------------------------------------------- |
| **क्लोरोस डेस्कटॉप** | स्थानीय रूप से स्थापित किया जाना चाहिए |
| **लाइसेंस** | क्लोरोस+ ([भुगतान योजना आवश्यक](https://cloud.mapir.camera/pricing)) |
| **ऑपरेटिंग सिस्टम** | विंडोज़ 10/11 (64-बिट) |
| **पायथन** | पायथन 3.7 या उच्चतर |
| **स्मृति** | न्यूनतम 8जीबी रैम (16जीबी अनुशंसित) |
| **इंटरनेट** | लाइसेंस सक्रियण के लिए आवश्यक |

{% संकेत शैली = "चेतावनी" %}
**लाइसेंस आवश्यकता**: पायथन एसडीके को एपीआई एक्सेस के लिए सशुल्क क्लोरोस+ सदस्यता की आवश्यकता होती है। मानक (निःशुल्क) योजनाओं में एपीआई/एसडीके पहुंच नहीं है। अपग्रेड करने के लिए [https://cloud.mapir.camera/pricing](https://cloud.mapir.camera/pricing) पर जाएं।
{%अंतसंकेत%}

## त्वरित शुरुआत

### स्थापना

पिप के माध्यम से स्थापित करें:

```bash
पिप इंस्टॉल क्लोरोस-एसडीके
```

{% संकेत शैली = "जानकारी" %}
**पहली बार सेटअप**: एसडीके का उपयोग करने से पहले, क्लोरोस, क्लोरोस (ब्राउज़र) या क्लोरोस सीएलआई खोलकर और अपने क्रेडेंशियल्स के साथ लॉग इन करके अपने क्लोरोस+ लाइसेंस को सक्रिय करें। इसे केवल एक बार ही करना होगा.
{%अंतसंकेत%}

### मूल उपयोग

किसी फ़ोल्डर को केवल कुछ पंक्तियों के साथ संसाधित करें:

```python
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

# एक-पंक्ति प्रसंस्करण
परिणाम = प्रक्रिया_फ़ोल्डर('C:\\DroneImages\\Flight001'')
```

### पूर्ण नियंत्रण

उन्नत वर्कफ़्लोज़ के लिए:

```python
Chloros_sdk से ChlorosLocal आयात करें

# SDK प्रारंभ करें
क्लोरोस = क्लोरोसलोकल()

# प्रोजेक्ट बनाएं
क्लोरोस.क्रिएट_प्रोजेक्ट('मायप्रोजेक्ट', कैमरा='सर्वे3एन_आरजीएन')

# छवियाँ आयात करें
cloros.import_images('C:\\DroneImages\\Flight001'')

# सेटिंग्स कॉन्फ़िगर करें
क्लोरोस.कॉन्फ़िगर(
    विग्नेट_सुधार = सत्य,
    परावर्तन_अंशांकन = सत्य,
    सूचकांक=["एनडीवीआई", "एनडीआरई", "जीएनडीवीआई"]
)

# छवियों को संसाधित करें
क्लोरोस.प्रोसेस(मोड='समानांतर', प्रतीक्षा=सत्य)
```

***

## इंस्टालेशन गाइड

### पूर्वावश्यकताएँ

एसडीके स्थापित करने से पहले, सुनिश्चित करें कि आपके पास:

1. **क्लोरोस डेस्कटॉप** स्थापित ([डाउनलोड](download.md))
2. **पायथन 3.7+** स्थापित ([python.org](https://www.python.org))
3. **एक्टिव क्लोरोस+ लाइसेंस** ([अपग्रेड](https://cloud.mapir.camera/pricing))

### पिप के माध्यम से स्थापित करें

**मानक स्थापना:**

```bash
पिप इंस्टॉल क्लोरोस-एसडीके
```

**प्रगति निगरानी समर्थन के साथ:**

```bash
पिप इंस्टाल क्लोरोस-एसडीके[प्रगति]
```

**विकास स्थापना:**

```bash
पिप इंस्टॉल क्लोरोस-एसडीके[देव]
```

### स्थापना सत्यापित करें

परीक्षण करें कि SDK सही ढंग से स्थापित है:

```python
क्लोरोस_एसडीके आयात करें
प्रिंट(f"क्लोरोस एसडीके संस्करण: {क्लोरोस_एसडीके.__संस्करण__}")
```

***

## पहली बार सेटअप

### लाइसेंस सक्रियण

एसडीके क्लोरोस, क्लोरोस (ब्राउज़र), और क्लोरोस सीएलआई के समान लाइसेंस का उपयोग करता है। GUI या CLI के माध्यम से एक बार सक्रिय करें:

1. **क्लोरोस या क्लोरोस (ब्राउज़र)** खोलें और उपयोगकर्ता <img src=”.gitbook/assets/icon_user.JPG” alt=”” data-size=”line”> टैब पर लॉगिन करें। या, **सीएलआई** खोलें।
2. अपना क्लोरोस+ क्रेडेंशियल दर्ज करें और लॉग इन करें
3. लाइसेंस स्थानीय रूप से कैश किया जाता है (रीबूट के दौरान बना रहता है)

{%संकेत शैली='सफलता'%}
**वन-टाइम सेटअप**: जीयूआई या सीएलआई के माध्यम से लॉग इन करने के बाद, एसडीके स्वचालित रूप से कैश्ड लाइसेंस का उपयोग करता है। किसी अतिरिक्त प्रमाणीकरण की आवश्यकता नहीं!
{%अंतसंकेत%}

### टेस्ट कनेक्शन

सत्यापित करें कि SDK क्लोरोस से कनेक्ट हो सकता है:

```python
Chloros_sdk से ChlorosLocal आयात करें

# एसडीके आरंभ करें (यदि आवश्यक हो तो बैकएंड ऑटो-स्टार्ट होता है)
क्लोरोस = क्लोरोसलोकल()

# स्थिति जाँचिए
स्थिति = क्लोरोस.गेट_स्टैटस()
प्रिंट(f"बैकएंड रनिंग: {स्थिति['रनिंग']}")
```

***

## एपीआई संदर्भ

### क्लोरोसलोकल क्लास

स्थानीय क्लोरोस छवि प्रसंस्करण के लिए मुख्य वर्ग।

#### कंस्ट्रक्टर

```python
क्लोरोसलोकल(
    api_url='http://localhost:5000', # बैकएंड यूआरएल
    ऑटो_स्टार्ट_बैकएंड=सही, # यदि नहीं चल रहा है तो ऑटो-स्टार्ट बैकएंड
    बैकएंड_exe=कोई नहीं, # बैकएंड पथ (स्वतः पता लगाया गया)
    टाइमआउट=30, # टाइमआउट का अनुरोध करें (सेकंड)
    बैकएंड_स्टार्टअप_टाइमआउट=60 # बैकएंड स्टार्टअप टाइमआउट
)
```

**पैरामीटर:**

| पैरामीटर | प्रकार | डिफ़ॉल्ट | विवरण |
| ------------------------- | ---- | ------------------------- | ------------------------------------- |
| `api_url` | स्ट्र | `"http://localhost:5000"` | स्थानीय क्लोरोस बैकएंड का यूआरएल |
| `ऑटो_स्टार्ट_बैकएंड` | बूल | 'सच' | यदि आवश्यक हो तो स्वचालित रूप से बैकएंड प्रारंभ करें |
| `बैकएंड_एक्सई` | स्ट्र | `कोई नहीं` (स्वतः पता लगाएं) | निष्पादन योग्य बैकएंड का पथ |
| `टाइमआउट` | int | `30` | सेकंड में टाइमआउट का अनुरोध करें |
| `बैकएंड_स्टार्टअप_टाइमआउट` | int | `60` | बैकएंड स्टार्टअप के लिए टाइमआउट (सेकंड) |

**उदाहरण:**

```python
# डिफ़ॉल्ट (ऑटो-स्टार्ट बैकएंड)
क्लोरोस = क्लोरोसलोकल()

# रनिंग बैकएंड से कनेक्ट करें
क्लोरोस = क्लोरोसलोकल(ऑटो_स्टार्ट_बैकएंड=गलत)

# कस्टम बैकएंड पथ
क्लोरोस = क्लोरोसलोकल(बैकएंड_exe='सी:/कस्टम/क्लोरोस-बैकएंड.exe')

# कस्टम टाइमआउट
क्लोरोस = क्लोरोसलोकल(टाइमआउट=60)
```

***

### तरीके

#### `क्रिएट_प्रोजेक्ट(प्रोजेक्ट_नाम, कैमरा=कोई नहीं)`

एक नया क्लोरोस प्रोजेक्ट बनाएं।

**पैरामीटर:**

| पैरामीटर | प्रकार | आवश्यक | विवरण |
| -------------- | ---- | -------- | -------------------------------------------------------- |
| `प्रोजेक्ट_नाम` | स्ट्र | हाँ | प्रोजेक्ट का नाम |
| 'कैमरा' | स्ट्र | नहीं | कैमरा टेम्प्लेट (उदा., "Survey3N\_RGN", "Survey3W\_OCN") |

**रिटर्न:** `dict` - प्रोजेक्ट निर्माण प्रतिक्रिया

**उदाहरण:**

```python
# बुनियादी परियोजना
क्लोरोस.क्रिएट_प्रोजेक्ट("ड्रोनफिल्ड_ए")

# कैमरा टेम्पलेट के साथ
क्लोरोस.क्रिएट_प्रोजेक्ट('ड्रोनफिल्ड_ए', कैमरा='सर्वे3एन_आरजीएन')
```

***

#### `import_images(folder_path, recursive=False)`

किसी फ़ोल्डर से छवियाँ आयात करें.

**पैरामीटर:**

| पैरामीटर | प्रकार | आवश्यक | विवरण |
| ------------- | -------- | -------- | ---------------------------------- |
| `फ़ोल्डर_पथ` | स्ट्र/पथ | हाँ | छवियों वाले फ़ोल्डर का पथ |
| 'पुनरावर्ती' | बूल | नहीं | सबफ़ोल्डर खोजें (डिफ़ॉल्ट: गलत) |

**रिटर्न:** `dict` - फ़ाइल संख्या के साथ परिणाम आयात करें

**उदाहरण:**

```python
#फ़ोल्डर से आयात करें
cloros.import_images('C:\\DroneImages\\Flight001'')

# पुनरावर्ती आयात करें
cloros.import_images('C:\\DroneImages', पुनरावर्ती = सत्य)
```

***

#### `कॉन्फ़िगर(**सेटिंग्स)`

प्रोसेसिंग सेटिंग्स कॉन्फ़िगर करें.

**पैरामीटर:**

| पैरामीटर | प्रकार | डिफ़ॉल्ट | विवरण |
| ------------------------- | ---- | ----------------------- | ------------------------------- |
| 'बहस करनेवाला' | स्ट्र | "उच्च गुणवत्ता (तेज़)" | डिबायर विधि |
| `विग्नेट_करेक्शन` | बूल | 'सच' | विग्नेट सुधार सक्षम करें |
| `प्रतिबिंब_अंशांकन` | बूल | 'सच' | परावर्तन अंशांकन सक्षम करें |
| `सूचकांक` | सूची | 'कोई नहीं' | गणना करने के लिए वनस्पति सूचकांक |
| `निर्यात_प्रारूप` | स्ट्र | "टीआईएफएफ (16-बिट)" | आउटपुट स्वरूप |
| `पीपीके` | बूल | 'झूठा' | पीपीके सुधार सक्षम करें |
| `कस्टम_सेटिंग्स` | हुक्म | 'कोई नहीं' | उन्नत कस्टम सेटिंग्स |

**निर्यात प्रारूप:**

* ``टीआईएफएफ (16-बिट)'' - जीआईएस/फोटोग्राममेट्री के लिए अनुशंसित
* ``टीआईएफएफ (32-बिट, प्रतिशत)'' - वैज्ञानिक विश्लेषण
* ``पीएनजी (8-बिट)'' - दृश्य निरीक्षण
* ``जेपीजी (8-बिट)'' - संपीड़ित आउटपुट

**उपलब्ध सूचकांक:**

NDVI, NDRE, GNDVI, OSAVI, CIG, EVI, SAVI, MSAVI, MTVI2, और बहुत कुछ।

**उदाहरण:**

```python
# बुनियादी विन्यास
क्लोरोस.कॉन्फ़िगर(
    विग्नेट_सुधार = सत्य,
    परावर्तन_अंशांकन = सत्य,
    सूचकांक=["एनडीवीआई", "एनडीआरई"]
)

# उन्नत कॉन्फ़िगरेशन
क्लोरोस.कॉन्फ़िगर(
    डिबेयर = "उच्च गुणवत्ता (तेज़)",
    विग्नेट_सुधार = सत्य,
    परावर्तन_अंशांकन = सत्य,
    पीपीके=सच,
    निर्यात_प्रारूप = "टीआईएफएफ (32-बिट, प्रतिशत)",
    सूचकांक=["एनडीवीआई", "एनडीआरई", "जीएनडीवीआई", "ओसावी", "सीआईजी"]
)
```

***

#### `प्रक्रिया(मोड=समानांतर', प्रतीक्षा=सही, प्रगति_कॉलबैक=कोई नहीं)'

प्रोजेक्ट छवियों को संसाधित करें.

**पैरामीटर:**

| पैरामीटर | प्रकार | डिफ़ॉल्ट | विवरण |
| ------------------- | -------- | ------------ | ----------------------------------------- |
| `मोड` | स्ट्र | `"समानांतर"` | प्रसंस्करण मोड: "समानांतर" या "धारावाहिक" |
| `रुको` | बूल | 'सच' | पूरा होने की प्रतीक्षा करें |
| `प्रगति_कॉलबैक` | कॉल करने योग्य | 'कोई नहीं' | प्रगति कॉलबैक फ़ंक्शन (प्रगति, संदेश) |
| `मतदान_अंतराल` | तैरना | `2.0` | प्रगति के लिए मतदान अंतराल (सेकंड) |

**रिटर्न:** `dict` - प्रसंस्करण परिणाम

{% संकेत शैली = "चेतावनी" %}
**समानांतर मोड**: क्लोरोस+ लाइसेंस की आवश्यकता है। स्वचालित रूप से आपके सीपीयू कोर (16 श्रमिकों तक) को स्केल करता है।
{%अंतसंकेत%}

**उदाहरण:**

```python
# सरल प्रसंस्करण
परिणाम = क्लोरोस.प्रक्रिया()

#प्रगति की निगरानी के साथ
def show_progress(प्रगति, संदेश):
    प्रिंट(f"[{प्रगति}%] {संदेश}")

क्लोरोस.प्रक्रिया(
    मोड = "समानांतर",
    प्रगति_कॉलबैक=शो_प्रगति,
    रुको=सच
)

# आग लगाओ और भूल जाओ (गैर-अवरुद्ध)
क्लोरोस.प्रक्रिया(प्रतीक्षा=गलत)
```

***

#### `get_config()`

वर्तमान प्रोजेक्ट कॉन्फ़िगरेशन प्राप्त करें.

**रिटर्न:** `dict` - वर्तमान प्रोजेक्ट कॉन्फ़िगरेशन

**उदाहरण:**

```python
कॉन्फिग = क्लोरोस.गेट_कॉन्फिग()
प्रिंट (कॉन्फिग['प्रोजेक्ट सेटिंग्स'])
```

***

#### `get_status()`

बैकएंड स्थिति की जानकारी प्राप्त करें.

**रिटर्न:** `dict` - बैकएंड स्थिति

**उदाहरण:**

```python
स्थिति = क्लोरोस.गेट_स्टैटस()
प्रिंट(f"चल रहा है: {स्थिति['चल रहा है']}")
प्रिंट(f"URL: {status['url']}")
```

***

#### `शटडाउन_बैकएंड()`

बैकएंड बंद करें (यदि एसडीके द्वारा शुरू किया गया हो)।

**उदाहरण:**

```python
क्लोरोस.शटडाउन_बैकएंड()
```

***

### सुविधा कार्य

#### `प्रक्रिया_फ़ोल्डर(फ़ोल्डर_पथ, **विकल्प)`

किसी फ़ोल्डर को संसाधित करने के लिए एक-पंक्ति सुविधा फ़ंक्शन।

**पैरामीटर:**

| पैरामीटर | प्रकार | डिफ़ॉल्ट | विवरण |
| ------------------------- | -------- | --------------- | ------------------------------ |
| `फ़ोल्डर_पथ` | स्ट्र/पथ | आवश्यक | छवियों वाले फ़ोल्डर का पथ |
| `प्रोजेक्ट_नाम` | स्ट्र | स्वतः उत्पन्न | प्रोजेक्ट का नाम |
| 'कैमरा' | स्ट्र | 'कोई नहीं' | कैमरा टेम्पलेट |
| `सूचकांक` | सूची | `["एनडीवीआई"]` | गणना करने के लिए सूचकांक |
| `विग्नेट_करेक्शन` | बूल | 'सच' | विग्नेट सुधार सक्षम करें |
| `प्रतिबिंब_अंशांकन` | बूल | 'सच' | परावर्तन अंशांकन सक्षम करें |
| `निर्यात_प्रारूप` | स्ट्र | "टीआईएफएफ (16-बिट)" | आउटपुट स्वरूप |
| `मोड` | स्ट्र | `"समानांतर"` | प्रोसेसिंग मोड |
| `प्रगति_कॉलबैक` | कॉल करने योग्य | 'कोई नहीं' | प्रगति कॉलबैक |

**रिटर्न:** `dict` - प्रसंस्करण परिणाम

**उदाहरण:**

```python
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

# सरल एक पंक्ति
परिणाम = प्रक्रिया_फ़ोल्डर('C:\\DroneImages\\Flight001'')

# कस्टम सेटिंग्स के साथ
परिणाम = प्रक्रिया_फ़ोल्डर(
    "C:\\DroneImages\\Flight001",
    प्रोजेक्ट_नाम = "फ़ील्ड_ए_सर्वेक्षण",
    कैमरा = "सर्वेक्षण3N_RGN",
    सूचकांक=["एनडीवीआई", "एनडीआरई", "जीएनडीवीआई"],
    मोड = "समानांतर"
)

#प्रगति की निगरानी के साथ
def show_progress(प्रगति, संदेश):
    प्रिंट(f"[{प्रगति}%] {संदेश}")

परिणाम = प्रक्रिया_फ़ोल्डर(
    "C:\\DroneImages\\Flight001",
    प्रगति_कॉलबैक=शो_प्रगति
)
```

***

## संदर्भ प्रबंधक समर्थन

एसडीके स्वचालित सफाई के लिए संदर्भ प्रबंधकों का समर्थन करता है:

```python
Chloros_sdk से ChlorosLocal आयात करें

# पूरा होने पर ऑटो-क्लीनअप
ChlorosLocal() के साथ क्लोरोस के रूप में:
    क्लोरोस.क्रिएट_प्रोजेक्ट("मायप्रोजेक्ट")
    क्लोरोस.import_images('C:\\Images'')
    क्लोरोस.कॉन्फ़िगर(सूचकांक=["एनडीवीआई"])
    क्लोरोस.प्रक्रिया()
# यहां बैकएंड अपने आप बंद हो जाता है
```

***

## संपूर्ण उदाहरण

### उदाहरण 1: बुनियादी प्रसंस्करण

डिफ़ॉल्ट सेटिंग्स वाले फ़ोल्डर को संसाधित करें:

```python
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

# डिफ़ॉल्ट सेटिंग्स के साथ प्रक्रिया करें
परिणाम = प्रक्रिया_फ़ोल्डर('C:\\Datasets\\Field_A_2025_01_15'')

प्रिंट करें(f"प्रसंस्करण पूर्ण: {परिणाम}")
```

***

### उदाहरण 2: कस्टम वर्कफ़्लो

प्रसंस्करण पाइपलाइन पर पूर्ण नियंत्रण:

```python
Chloros_sdk से ChlorosLocal आयात करें

# SDK प्रारंभ करें
क्लोरोस = क्लोरोसलोकल()

# कैमरा टेम्पलेट के साथ प्रोजेक्ट बनाएं
क्लोरोस.क्रिएट_प्रोजेक्ट('रिसर्च_प्लॉट_ए', कैमरा='सर्वे3एन_आरजीएन')

# छवियाँ आयात करें
आयात_परिणाम = क्लोरोस.import_images('C:\\Research\\PlotA'')
प्रिंट(f"आयातित {len(import_results.get('files', []))} छवियाँ")

# उन्नत सेटिंग्स कॉन्फ़िगर करें
क्लोरोस.कॉन्फ़िगर(
    डिबेयर = "उच्च गुणवत्ता (तेज़)",
    विग्नेट_सुधार = सत्य,
    परावर्तन_अंशांकन = सत्य,
    पीपीके=गलत,
    निर्यात_प्रारूप = "टीआईएफएफ (16-बिट)",
    सूचकांक=["एनडीवीआई", "एनडीआरई", "जीएनडीवीआई", "ओएसएवीआई"]
)

#प्रगति की निगरानी के साथ प्रक्रिया
def show_progress(प्रगति, संदेश):
    प्रिंट(f"प्रगति: {प्रगति}% - {संदेश}")

क्लोरोस.प्रक्रिया(
    मोड = "समानांतर",
    प्रगति_कॉलबैक=शो_प्रगति,
    रुको=सच
)

प्रिंट करें ("प्रसंस्करण पूर्ण!")
```

***

### उदाहरण 3: एकाधिक फ़ोल्डरों का बैच प्रसंस्करण

एकाधिक उड़ान डेटासेट संसाधित करें:

```python
Chloros_sdk से ChlorosLocal आयात करें
पाथलिब आयात पथ से

# SDK को एक बार प्रारंभ करें
क्लोरोस = क्लोरोसलोकल()

# उड़ान फ़ोल्डरों की सूची
उड़ानें = [
    "C:\\डेटासेट\\Flight_001",
    "C:\\डेटासेट\\Flight_002",
    "C:\\डेटासेट\\Flight_003"
]

उड़ानों में Flight_path के लिए:
    उड़ान_नाम = पथ(उड़ान_पथ).नाम
    प्रिंट(f"\n{'='*60}")
    प्रिंट(f"प्रसंस्करण: {flight_name}")
    प्रिंट('='*60)
    
    कोशिश करना:
        # प्रोजेक्ट बनाएं
        क्लोरोस.क्रिएट_प्रोजेक्ट(उड़ान_नाम, कैमरा='सर्वेक्षण3एन_आरजीएन')
        
        # छवियाँ आयात करें
        क्लोरोस.इम्पोर्ट_इमेज(उड़ान_पथ)
        
        # कॉन्फ़िगर करें
        क्लोरोस.कॉन्फ़िगर(
            विग्नेट_सुधार = सत्य,
            परावर्तन_अंशांकन = सत्य,
            सूचकांक=["एनडीवीआई", "एनडीआरई", "जीएनडीवीआई"]
        )
        
        # प्रक्रिया
        क्लोरोस.प्रोसेस(मोड='समानांतर', प्रतीक्षा=सत्य)
        
        प्रिंट(f"✓ {flight_name} सफलतापूर्वक पूरा हुआ")
    
    ई के रूप में अपवाद को छोड़कर:
        प्रिंट(f"✗ {flight_name} विफल: {e}")

प्रिंट करें ("\n" + "=*60)
प्रिंट करें ("सभी उड़ानें संसाधित हो गईं!")
```

***

### उदाहरण 4: अनुसंधान पाइपलाइन एकीकरण

डेटा विश्लेषण के साथ क्लोरोस को एकीकृत करें:

```python
Chloros_sdk से ChlorosLocal आयात करें
पांडा को पीडी के रूप में आयात करें
matplotlib.pyplot को plt के रूप में आयात करें

# क्लोरोस आरंभ करें
क्लोरोस = क्लोरोसलोकल()

# फ़ील्ड सर्वेक्षण डेटा
सर्वेक्षण = [
    {"नाम": "प्लॉट_ए", "फ़ोल्डर": "सी:\\रिसर्च\\प्लॉटए", "बायोमास": 4500},
    {"नाम": "प्लॉट_बी", "फ़ोल्डर": "सी:\\रिसर्च\\प्लॉटबी", "बायोमास": 3800},
    {"नाम": "प्लॉट_सी", "फ़ोल्डर": "सी:\\रिसर्च\\प्लॉटसी", "बायोमास": 5200}
]

परिणाम = []

सर्वेक्षणों में सर्वेक्षण के लिए:
    #क्लोरोस के साथ प्रक्रिया
    क्लोरोस.क्रिएट_प्रोजेक्ट(सर्वेक्षण['नाम'])
    क्लोरोस.इम्पोर्ट_इमेज(सर्वेक्षण['फ़ोल्डर'])
    क्लोरोस.कॉन्फ़िगर(सूचकांक=["एनडीवीआई", "एनडीआरई"])
    क्लोरोस.प्रोसेस(मोड='समानांतर', प्रतीक्षा=सत्य)
    
    #परिणाम प्राप्त करें
    कॉन्फिग = क्लोरोस.गेट_कॉन्फिग()
    
    # NDVI मान निकालें (उदाहरण - अपनी आवश्यकताओं के आधार पर समायोजित करें)
    # वास्तविक कार्यान्वयन में, आप संसाधित TIFF फ़ाइलें पढ़ेंगे
    
    परिणाम.जोड़ें({
        'कथानक': सर्वेक्षण['नाम'],
        'बायोमास': सर्वेक्षण['बायोमास'],
        # अपना NDVI निष्कर्षण यहां जोड़ें
    })

#सांख्यिकीय विश्लेषण
डीएफ = पीडी.डेटाफ़्रेम(परिणाम)
प्रिंट करें ("\nपरिणाम:")
प्रिंट(डीएफ)

# सहसंबंध कथानक बनाएं
# plt.scatter(df['ndvi'], df['बायोमास'])
# plt.xlabel('NDVI')
# plt.ylabel('बायोमास (किलो/हेक्टेयर)')
# plt.title('NDVI बनाम बायोमास सहसंबंध')
# plt.शो()
```

***

### उदाहरण 5: कस्टम प्रगति निगरानी

लॉगिंग के साथ उन्नत प्रगति ट्रैकिंग:

```python
Chloros_sdk से ChlorosLocal आयात करें
दिनांक समय से दिनांक समय आयात करें
लॉगिंग आयात करें

# सेटअप लॉगिंग
लॉगिंग.बेसिककॉन्फ़िग(
    फ़ाइल नाम=f'processing_{datetime.now():%Y%m%d_%H%M%S}.log',
    लेवल=लॉगिंग.जानकारी,
    प्रारूप='%(asctime)s - %(संदेश)s'
)

# लॉगिंग के साथ प्रगति कॉलबैक
def log_progress(प्रगति, संदेश):
    log_msg = f"[{प्रगति}%] {संदेश}"
    लॉगिंग.जानकारी(log_msg)
    प्रिंट(log_msg)

# लॉगिंग के साथ प्रक्रिया करें
क्लोरोस = क्लोरोसलोकल()
क्लोरोस.क्रिएट_प्रोजेक्ट("लॉगप्रोसेस")
cloros.import_images('C:\\DroneImages'')
क्लोरोस.कॉन्फ़िगर(सूचकांक=["एनडीवीआई", "एनडीआरई"])

loging.info('प्रसंस्करण प्रारंभ हो रहा है...')
क्लोरोस.प्रक्रिया(
    मोड = "समानांतर",
    प्रगति_कॉलबैक=लॉग_प्रगति,
    रुको=सच
)
loging.info("प्रक्रिया पूर्ण!")
```

***

### उदाहरण 6: त्रुटि प्रबंधन

उत्पादन उपयोग के लिए मजबूत त्रुटि प्रबंधन:

```python
Chloros_sdk से ChlorosLocal आयात करें
क्लोरोस_एसडीके.एक्सेप्शन आयात से (
    क्लोरोसत्रुटि,
    क्लोरोसबैकएंडत्रुटि,
    क्लोरोसलाइसेंसत्रुटि,
    क्लोरोसप्रोसेसिंगत्रुटि
)

डीईएफ़ प्रक्रिया_सुरक्षित रूप से (फ़ोल्डर_पथ):
    """व्यापक त्रुटि प्रबंधन के साथ प्रक्रिया"""
    कोशिश करना:
        ChlorosLocal() के साथ क्लोरोस के रूप में:
            क्लोरोस.क्रिएट_प्रोजेक्ट("सेफप्रोसेस")
            क्लोरोस.import_images(फ़ोल्डर_पथ)
            क्लोरोस.कॉन्फ़िगर(सूचकांक=["एनडीवीआई"])
            क्लोरोस.प्रक्रिया()
            
        सच लौटें, "सफलता"
    
    ChlorosLicenseError को e के रूप में छोड़कर:
        गलत वापसी, f"लाइसेंस त्रुटि: {e}। Cloud.mapir.camera/pricing पर Chloros+ में अपग्रेड करें"
    
    ChlorosBackendError को e के रूप में छोड़कर:
        गलत वापसी, f"बैकएंड त्रुटि: {e}। सुनिश्चित करें कि क्लोरोस डेस्कटॉप स्थापित है।"
    
    ई के रूप में ChlorosProcessingError को छोड़कर:
        गलत वापसी, f"प्रसंस्करण त्रुटि: {e}"
    
    FileNotFoundError को e के रूप में छोड़कर:
        गलत वापसी, f"फ़ोल्डर नहीं मिला: {e}"
    
    ई के रूप में ChlorosError को छोड़कर:
        गलत वापसी, f"क्लोरोस त्रुटि: {e}"
    
    ई के रूप में अपवाद को छोड़कर:
        गलत वापसी, f"अप्रत्याशित त्रुटि: {e}"

# सुरक्षित फ़ंक्शन का उपयोग करें
सफलता, संदेश = प्रक्रिया_सुरक्षित रूप से('C:\\DroneImages\\Flight001'')
यदि सफलता:
    प्रिंट(f"✓ {संदेश}")
अन्य:
    प्रिंट(f"✗ {संदेश}")
```

***

### उदाहरण 7: कमांड-लाइन टूल

SDK के साथ एक कस्टम CLI टूल बनाएं:

```python
#!/usr/bin/env पायथन
"""
कस्टम क्लोरोस सीएलआई उपकरण
कमांड लाइन से एकाधिक फ़ोल्डरों को संसाधित करें
"""

आयात sys
आयात argparse
पाथलिब आयात पथ से
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

डीईएफ़ मुख्य():
    पार्सर = argparse.ArgumentParser(विवरण='कस्टम क्लोरोस प्रोसेसर')
    parser.add_argument('फ़ोल्डर', nargs='+', मदद='प्रक्रिया हेतु फ़ोल्डर')
    parser.add_argument('--indices', nargs='+', default=['NDVI'],
                       सहायता='गणना के लिए सूचकांक (डिफ़ॉल्ट: एनडीवीआई)')
    parser.add_argument('--कैमरा', डिफ़ॉल्ट=कोई नहीं,
                       सहायता='कैमरा टेम्प्लेट')
    parser.add_argument('--format', default='TIFF (16-बिट)',
                       सहायता='निर्यात प्रारूप')
    
    तर्क = पार्सर.parse_args()
    
    सफल = []
    विफल = []
    
    args.folders में फ़ोल्डर के लिए:
        फ़ोल्डर_पथ = पथ(फ़ोल्डर)
        
        यदि नहीं तो फ़ोल्डर_पथ.मौजूद है():
            print(f"✗ स्किपिंग {फ़ोल्डर}: नहीं मिला")
            विफल.जोड़ें(फ़ोल्डर)
            जारी रखना
        
        प्रिंट(f"\nप्रसंस्करण: {folder_path.name}...")
        
        कोशिश करना:
            प्रक्रिया_फ़ोल्डर(
                फ़ोल्डर_पथ,
                कैमरा=args.कैमरा,
                सूचकांक=args.सूचकांक,
                निर्यात_प्रारूप = args.format
            )
            प्रिंट(f"✓ {folder_path.name} पूर्ण")
            सफल.जोड़ें(फ़ोल्डर)
        
        ई के रूप में अपवाद को छोड़कर:
            प्रिंट(f"✗ {folder_path.name} विफल: {e}")
            विफल.जोड़ें(फ़ोल्डर)
    
    # सारांश
    प्रिंट(f"\n{'='*60}")
    प्रिंट(f"सारांश: {len(सफल)} सफल, {len(असफल)} विफल")
    
    यदि विफल नहीं हुआ तो 0 लौटाएँ अन्यथा 1

यदि __नाम__ == '__मुख्य__':
    sys.exit(मुख्य())
```

**उपयोग:**

```bash
पायथन my_processor.py "C:\Flight001" "C:\Flight002" --indices NDVI NDRE GNDVI
```

***

## एक्सेप्शन हेंडलिंग

एसडीके विभिन्न त्रुटि प्रकारों के लिए विशिष्ट अपवाद वर्ग प्रदान करता है:

### अपवाद पदानुक्रम

```python
ChlorosError # आधार अपवाद
├── ChlorosBackendError # बैकएंड स्टार्टअप/कनेक्शन समस्याएँ
├── ChlorosLicenseError # लाइसेंस सत्यापन मुद्दे
├── ChlorosConnectionError # नेटवर्क/कनेक्शन विफलता
├── ChlorosProcessingError # छवि प्रसंस्करण विफलताएँ
├── ChlorosAuthenticationError # प्रमाणीकरण विफलता
└── ChlorosConfigurationError # कॉन्फ़िगरेशन त्रुटियाँ
```

### अपवाद उदाहरण

```python
Chloros_sdk से ChlorosLocal आयात करें
क्लोरोस_एसडीके.एक्सेप्शन आयात से *

कोशिश करना:
    क्लोरोस = क्लोरोसलोकल()
    क्लोरोस.प्रक्रिया()

ChlorosLicenseError को छोड़कर:
    प्रिंट करें ("क्लोरोस+ लाइसेंस आवश्यक है। Cloud.mapir.camera/pricing पर अपग्रेड करें")

ChlorosBackendError को छोड़कर:
    प्रिंट करें ("बैकएंड प्रारंभ होने में विफल रहा। सुनिश्चित करें कि क्लोरोस डेस्कटॉप स्थापित है।")

ई के रूप में ChlorosProcessingError को छोड़कर:
    प्रिंट(f"प्रसंस्करण विफल: {e}")

ई के रूप में ChlorosError को छोड़कर:
    प्रिंट(f"सामान्य क्लोरोस त्रुटि: {e}")
```

***

## उन्नत विषय

### कस्टम बैकएंड कॉन्फ़िगरेशन

कस्टम बैकएंड स्थान या कॉन्फ़िगरेशन का उपयोग करें:

```python
क्लोरोस = क्लोरोसलोकल(
    बैकएंड_exe = "C:\\Custom\\क्लोरोस-बैकएंड.exe",
    api_url='http://localhost:5001', # कस्टम पोर्ट
    टाइमआउट=60, # लंबा टाइमआउट
    बैकएंड_स्टार्टअप_टाइमआउट=120 # 2 मिनट का स्टार्टअप
)
```

### नॉन-ब्लॉकिंग प्रोसेसिंग

प्रसंस्करण प्रारंभ करें और अन्य कार्य जारी रखें:

```python
# प्रसंस्करण प्रारंभ करें (गैर-अवरुद्ध)
क्लोरोस.प्रक्रिया(प्रतीक्षा=गलत)

# दूसरा काम यहीं करो...
प्रिंट करें ("पृष्ठभूमि में प्रसंस्करण प्रारंभ हुआ...")

# स्थिति बाद में जांचें
आयात समय
जबकि सत्य:
    स्थिति = क्लोरोस.गेट_कॉन्फिग()
    यदि status.get('processing_complete'):
        तोड़ना
    समय.नींद(5)

प्रिंट करें ("प्रसंस्करण पूर्ण!")
```

### मेमोरी प्रबंधन

बड़े डेटासेट के लिए, बैचों में प्रक्रिया करें:

```python
पाथलिब आयात पथ से

बेस_फ़ोल्डर = पथ ("सी:\\लार्जडेटासेट")
बैच_आकार = 100

# सभी छवि फ़ाइलें प्राप्त करें
छवियाँ = सूची(base_folder.glob("*.RAW"))

# बैचों में प्रक्रिया
i के लिए रेंज (0, len(images), बैच_साइज़):
    बैच = छवियाँ[i:i+batch_size]
    बैच_फ़ोल्डर = बेस_फ़ोल्डर / एफ"बैच_{i//बैच_साइज़}"
    
    # बैच फ़ोल्डर बनाएं और छवियों को स्थानांतरित करें
    # ... (कार्यान्वयन विवरण)
    
    # प्रक्रिया बैच
    प्रोसेस_फ़ोल्डर(बैच_फ़ोल्डर)
```

***

## समस्या निवारण

### बैकएंड प्रारंभ नहीं हो रहा है

**समस्या:** SDK बैकएंड प्रारंभ करने में विफल रहता है

**समाधान:**

1. सत्यापित करें कि क्लोरोस डेस्कटॉप स्थापित है:

```python
ओएस आयात करें
बैकएंड_पथ = r"C:\प्रोग्राम फ़ाइलें\MAPIR\क्लोरोस\संसाधन\बैकएंड\क्लोरोस-बैकएंड.exe"
प्रिंट(f"बैकएंड मौजूद है: {os.path.exists(backend_path)}")
```

2. जांचें कि विंडोज़ फ़ायरवॉल अवरुद्ध तो नहीं हो रहा है
3. मैन्युअल बैकएंड पथ आज़माएँ:

```python
क्लोरोस = क्लोरोसलोकल(बैकएंड_exe='C:\\Path\\To\\क्लोरोस-बैकएंड.exe'')
```

***

### लाइसेंस का पता नहीं चला

**मुद्दा:** एसडीके ने लाइसेंस गुम होने की चेतावनी दी है

**समाधान:**

1. क्लोरोस, क्लोरोस (ब्राउज़र) या क्लोरोस सीएलआई खोलें और लॉगिन करें।
2. सत्यापित करें कि लाइसेंस कैश किया गया है:

```python
पाथलिब आयात पथ से
ओएस आयात करें

# कैश स्थान जांचें (विंडोज़)
कैश_पथ = पथ(os.getenv('APPDATA')) / 'क्लोरोस' / 'कैश'
प्रिंट(f"कैश मौजूद है: {cache_path.exists()}")
```

3. समर्थन से संपर्क करें: info@mapir.camera

***

### आयात त्रुटियाँ

**समस्या:** `ModuleNotFoundError: 'क्लोरोस_एसडीके' नाम का कोई मॉड्यूल नहीं`

**समाधान:**

```bash
# स्थापना सत्यापित करें
पिप शो क्लोरोस-एसडीके

# यदि आवश्यक हो तो पुनः स्थापित करें
पिप अनइंस्टॉल क्लोरोस-एसडीके
पिप इंस्टॉल क्लोरोस-एसडीके

# पायथन वातावरण की जाँच करें
पायथन -सी "आयात sys; प्रिंट (sys.path)"
```

***

### प्रोसेसिंग टाइमआउट

**समस्या:** प्रसंस्करण समय समाप्त

**समाधान:**

1. टाइमआउट बढ़ाएँ:

```python
क्लोरोस = क्लोरोसलोकल(टाइमआउट=120) # 2 मिनट
```

2. छोटे बैचों में प्रक्रिया करें
3. उपलब्ध डिस्क स्थान की जाँच करें
4. सिस्टम संसाधनों की निगरानी करें

***

### पोर्ट पहले से ही उपयोग में है

**समस्या:** बैकएंड पोर्ट 5000 पर कब्जा

**समाधान:**

```python
# विभिन्न पोर्ट का उपयोग करें
क्लोरोस = क्लोरोसलोकल(api_url='http://localhost:5001')
```

या विरोधाभासी प्रक्रिया ढूंढें और बंद करें:

```powershell
# पावरशेल
Get-NetTCPConnection -LocalPort 5000
```

***

## प्रदर्शन युक्तियाँ

### प्रसंस्करण गति को अनुकूलित करें

1. **समानांतर मोड का उपयोग करें** (क्लोरोस+ की आवश्यकता है)

```python
क्लोरोस.प्रोसेस(मोड='समानांतर') # 16 श्रमिकों तक
```

2. **आउटपुट रिज़ॉल्यूशन कम करें** (यदि स्वीकार्य हो)

```python
cloros.configure(export_format='PNG (8-बिट)') # TIFF से भी तेज़
```

3. **अनावश्यक सूचकांक अक्षम करें**

```python
# केवल आवश्यक सूचकांकों की गणना करें
cloros.configure(indices=["NDVI"]) # सभी सूचकांक नहीं
```

4. **एसएसडी पर प्रक्रिया** (एचडीडी नहीं)

***

### मेमोरी अनुकूलन

बड़े डेटासेट के लिए:

```python
# सभी को एक साथ करने के बजाय बैचों में प्रक्रिया करें
# उन्नत विषयों में "मेमोरी प्रबंधन" देखें
```

***

### पृष्ठभूमि प्रसंस्करण

अन्य कार्यों के लिए पायथन को मुक्त करें:

```python
क्लोरोस.प्रोसेस(प्रतीक्षा=गलत) # गैर-अवरुद्ध

# अन्य कार्य जारी रखें
# ...
```

***

## एकीकरण उदाहरण

### Django एकीकरण

```python
#views.py
django.http से JsonResponse आयात करें
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

डीईएफ़ प्रक्रिया_इमेज_व्यू(अनुरोध):
    यदि अनुरोध.विधि == 'पोस्ट':
        फ़ोल्डर_पथ = अनुरोध.POST.get('फ़ोल्डर_पथ')
        
        कोशिश करना:
            परिणाम = प्रक्रिया_फ़ोल्डर(फ़ोल्डर_पथ)
            वापसी JsonResponse({'सफलता': सत्य, 'परिणाम': परिणाम})
        ई के रूप में अपवाद को छोड़कर:
            वापसी JsonResponse({'सफलता': गलत, 'त्रुटि': str(e)})
```

### फ्लास्क एपीआई

```python
#app.py
फ्लास्क से आयात फ्लास्क, अनुरोध, jsonify
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

ऐप = फ्लास्क(__नाम__)

@app.route('/api/process', पद्धतियाँ=['POST'])
डीईएफ़ प्रक्रिया():
    डेटा = request.get_json()
    फ़ोल्डर_पथ = data.get('फ़ोल्डर_पथ')
    
    कोशिश करना:
        परिणाम = प्रक्रिया_फ़ोल्डर(फ़ोल्डर_पथ)
        वापसी jsonify({'सफलता': सत्य, 'परिणाम': परिणाम})
    ई के रूप में अपवाद को छोड़कर:
        वापसी jsonify({'सफलता': गलत, 'त्रुटि': str(e)}), 500

यदि __नाम__ == '__मुख्य__':
    ऐप.रन()
```

### ज्यूपिटर नोटबुक

```python
# नोटबुक.ipynb
Chloros_sdk से ChlorosLocal आयात करें
matplotlib.pyplot को plt के रूप में आयात करें

# आरंभ करें
क्लोरोस = क्लोरोसलोकल()

# प्रक्रिया
क्लोरोस.क्रिएट_प्रोजेक्ट("ज्यूपिटरटेस्ट")
क्लोरोस.इम्पोर्ट_इमेज('सी:\\डेटा')
क्लोरोस.कॉन्फ़िगर(सूचकांक=["एनडीवीआई"])

#नोटबुक में प्रगति
IPython.display से क्लियर_आउटपुट आयात करें

डीईएफ़ नोटबुक_प्रगति(प्रगति, संदेश):
    क्लियर_आउटपुट(प्रतीक्षा=सही)
    प्रिंट(f"प्रगति: {प्रगति}%")
    प्रिंट(संदेश)

क्लोरोस.प्रक्रिया(प्रगति_कॉलबैक=नोटबुक_प्रगति)

#परिणाम देखें
# ... (आपका विज़ुअलाइज़ेशन कोड)
```

***

## अक्सर पूछे जाने वाले प्रश्न

### प्रश्न: क्या एसडीके को इंटरनेट कनेक्शन की आवश्यकता है?

**ए:** केवल प्रारंभिक लाइसेंस सक्रियण के लिए। क्लोरोस, क्लोरोस (ब्राउज़र) या क्लोरोस सीएलआई के माध्यम से लॉग इन करने के बाद लाइसेंस स्थानीय रूप से कैश किया जाता है और 30 दिनों के लिए ऑफ़लाइन काम करता है।

***

### प्रश्न: क्या मैं जीयूआई के बिना सर्वर पर एसडीके का उपयोग कर सकता हूं?

**ए:** हाँ! आवश्यकताएं:

*विंडोज सर्वर 2016 या बाद का संस्करण
*क्लोरोस स्थापित (एक बार)
* किसी भी मशीन पर लाइसेंस सक्रिय (कैश्ड लाइसेंस सर्वर पर कॉपी किया गया)

***

### प्रश्न: डेस्कटॉप, सीएलआई और एसडीके के बीच क्या अंतर है?

| फ़ीचर | डेस्कटॉप जीयूआई | सीएलआई कमांड लाइन | पायथन एसडीके |
| --------------- | ----------- | ---------------- | ----------- |
| **इंटरफ़ेस** | प्वाइंट-क्लिक करें | आदेश | पायथन एपीआई |
| **के लिए सर्वश्रेष्ठ** | दृश्य कार्य | स्क्रिप्टिंग | एकीकरण |
| **स्वचालन** | सीमित | अच्छा | उत्कृष्ट |
| **लचीलापन** | बुनियादी | अच्छा | अधिकतम |
| **लाइसेंस** | क्लोरोस+ | क्लोरोस+ | क्लोरोस+ |

***

### प्रश्न: क्या मैं एसडीके के साथ निर्मित ऐप्स वितरित कर सकता हूं?

**ए:** एसडीके कोड को आपके एप्लिकेशन में एकीकृत किया जा सकता है, लेकिन:

* अंतिम उपयोगकर्ताओं को क्लोरोस स्थापित करने की आवश्यकता है
* अंतिम उपयोगकर्ताओं को सक्रिय क्लोरोस+ लाइसेंस की आवश्यकता होती है
* वाणिज्यिक वितरण के लिए ओईएम लाइसेंसिंग की आवश्यकता होती है

OEM पूछताछ के लिए info@mapir.camera से संपर्क करें।

***

### प्रश्न: मैं एसडीके को कैसे अपडेट करूं?

```bash
पिप इंस्टाल--अपग्रेड क्लोरोस-एसडीके
```

***

### प्रश्न: संसाधित छवियाँ कहाँ सहेजी जाती हैं?

डिफ़ॉल्ट रूप से, प्रोजेक्ट पथ में:

```
प्रोजेक्ट_पथ/
└── मेराप्रोजेक्ट/
    └── सर्वे3एन_आरजीएन/ # संसाधित आउटपुट
```

***

### प्रश्न: क्या मैं निर्धारित समय पर चल रही पायथन स्क्रिप्ट से छवियों को संसाधित कर सकता हूं?

**ए:** हाँ! पायथन स्क्रिप्ट के साथ विंडोज टास्क शेड्यूलर का उपयोग करें:

```python
# शेड्यूल_प्रोसेसिंग.py
क्लोरोस_एसडीके आयात प्रक्रिया_फ़ोल्डर से

# आज की उड़ानें संसाधित करें
परिणाम = प्रक्रिया_फ़ोल्डर ("सी:\\उड़ान\\आज")
```

प्रतिदिन चलने के लिए टास्क शेड्यूलर के माध्यम से शेड्यूल करें।

***

### प्रश्न: क्या एसडीके एसिंक/प्रतीक्षा का समर्थन करता है?

**ए:** वर्तमान संस्करण समकालिक है। एसिंक व्यवहार के लिए, `wait=False` का उपयोग करें या अलग थ्रेड में चलाएँ:

```python
थ्रेडिंग आयात करें

डीईएफ़ प्रक्रिया_थ्रेड():
    क्लोरोस.प्रक्रिया()

थ्रेड = थ्रेडिंग.थ्रेड(लक्ष्य=प्रोसेस_थ्रेड)
थ्रेड.स्टार्ट()

# अन्य कार्य जारी रखें...
```

***

## मदद प्राप्त करें

### दस्तावेज़ीकरण

* **एपीआई संदर्भ**: यह पृष्ठ

### सहायता चैनल

* **ईमेल**: info@mapir.camera
* **वेबसाइट**: [https://www.mapir.camera/community/contact](https://www.mapir.camera/community/contact)
* **मूल्य निर्धारण**: [https://cloud.mapir.camera/pricing](https://cloud.mapir.camera/pricing)

### नमूना कोड

यहां सूचीबद्ध सभी उदाहरण परीक्षणित और उत्पादन के लिए तैयार हैं। उन्हें कॉपी करें और अपने उपयोग के मामले में अनुकूलित करें।

***

## लाइसेंस

**मालिकाना सॉफ्टवेयर** - कॉपीराइट (सी) 2025 एमएपीआईआर इंक।

एसडीके को एक सक्रिय क्लोरोस+ सदस्यता की आवश्यकता है। अनधिकृत उपयोग, वितरण या संशोधन निषिद्ध है।
